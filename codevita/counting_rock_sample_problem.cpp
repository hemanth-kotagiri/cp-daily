#include<bits/stdc++.h>
using namespace std;


int main() {
    long R, S, i;
    cin >> S >> R;
    vector<int> sizes;
    for(i = 0; i < S; i++) {
        long curr;
        cin >> curr;
        sizes.push_back(curr);
    }
}
